{
  "name": "lingualeap",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered language learning platform with hybrid speech recognition",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "type-check": "npm run type-check --workspace=backend && npm run type-check --workspace=frontend",
    "bootstrap": "npm install && npm run build:shared",
    "build:shared": "npm run build --workspace=shared",
    "clean": "npm run clean --workspace=backend && npm run clean --workspace=frontend && npm run clean --workspace=shared",
    "setup:local": "./setup-local.sh",
    "test:local": "./test-local.sh",
    "db:start": "docker-compose -f docker-compose.local.yml up -d postgres redis",
    "db:stop": "docker-compose -f docker-compose.local.yml down",
    "db:reset": "docker-compose -f docker-compose.local.yml down -v && docker-compose -f docker-compose.local.yml up -d postgres redis",
    "db:admin": "docker-compose -f docker-compose.local.yml --profile tools up -d pgladmin redis-commander",
    "demo:start": "concurrently \"npm run db:start\" \"sleep 10 && npm run dev\"",
    "demo:test": "npm run test:local && echo '\n🎉 Demo environment ready! Open http://localhost:3000'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.0",
    "typescript": "^5.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "language-learning",
    "speech-recognition",
    "ai",
    "education",
    "react",
    "typescript",
    "nodejs",
    "postgresql"
  ],
  "author": "LinguaLeap Team",
  "license": "MIT"
}
